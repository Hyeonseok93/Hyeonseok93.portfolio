/* Previously: transform:scale fixed 1920×913 canvas above 1101×821.
   Desktop shrink is now fluid rem via site.css (--ui-scale).
   Tablet/phone keep stacked layouts in page CSS. This file is a no-op
   so existing data-slide-fit markup stays harmless. */
(() => {})();
